version: '3.8'

networks:
  application:
    driver: bridge

services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile   # 👈 Updated path
    container_name: laravel-app
    volumes:
      - .:/var/www/html
    depends_on:
      - mysql
    networks:
      - application

  nginx:
    image: nginx:latest
    container_name: laravel-nginx
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - application

  mysql:
    image: mysql:5.7
    container_name: laravel-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravell
      MYSQL_USER: laravell
      MYSQL_PASSWORD: secret
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - application

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: laravel-phpmyadmin
    restart: always
    ports:
      - "8081:80"
    environment:
      PMA_HOST: laravel-mysql
      PMA_PORT: 3306
      PMA_USER: laravell
      PMA_PASSWORD: secret
    depends_on:
      - mysql
    networks:
      - application

volumes:
  mysql-data:
